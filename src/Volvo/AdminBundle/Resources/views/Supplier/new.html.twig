{% extends 'VolvoFrontBundle:Default:index.html.twig' %}

{% block content -%}
    <h1>Supplier creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('supplier') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
